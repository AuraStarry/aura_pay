# =========================================
# Aura Pay Environment Variables
# =========================================
# Copy this file to `.env.local` for local development.
# Required vars must be present in local/dev/prod.

# -----------------------------------------
# Supabase (Required)
# -----------------------------------------
# Server-side credentials used by API routes.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-service-role-key

# Public credentials exposed to browser bundle.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_KEY=your-anon-public-key

# -----------------------------------------
# Admin API Access Gate (Required for /admin and /api/products)
# -----------------------------------------
# viewer token: can read products
ADMIN_READ_TOKEN=replace-with-strong-viewer-token
# admin token: can create/update/delete products
ADMIN_WRITE_TOKEN=replace-with-strong-admin-token

# -----------------------------------------
# Paddle (Optional for current MVP, Required in Paddle phase)
# -----------------------------------------
# Vendor auth
PADDLE_API_KEY=your-paddle-api-key
PADDLE_WEBHOOK_SECRET=your-paddle-webhook-secret
PADDLE_ENV=sandbox

# Price/product mapping and checkout host
PADDLE_DEFAULT_CURRENCY=USD
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------
# Notes
# -----------------------------------------
# - Keep secrets only in server-side env.
# - Never expose service role keys in client code.
# - On Vercel, define the same variable names in Project Settings.
